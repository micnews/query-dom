{
  "name": "read-only-dom",
  "version": "0.0.0",
  "description": "read-only & somewhat standards compliant subset of the DOM",
  "main": "index.js",
  "scripts": {
    "test": "semistandard | snazzy && ava",
    "benchmark:build": "mkdir -p dist && babel benchmark/benchmark.js --out-file dist/benchmark.js",
    "benchmark": "npm-run-all --parallel benchmark:build build && node dist/benchmark.js",
    "build": "mkdir -p dist && babel index.js --out-file dist/index.js",
    "prerelease": "rm -r dist && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/micnews/read-only-dom.git"
  },
  "author": "mic.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/micnews/read-only-dom/issues"
  },
  "homepage": "https://github.com/micnews/read-only-dom#readme",
  "devDependencies": {
    "ava": "^0.11.0",
    "babel-cli": "^6.4.5",
    "babel-core": "^6.4.5",
    "babel-preset-es2015": "^6.3.13",
    "jsdom": "^8.0.1",
    "minidom": "^1.0.0",
    "npm-run-all": "^1.5.1",
    "semistandard": "^7.0.5",
    "snazzy": "^2.0.1"
  },
  "dependencies": {
    "dom-style-parser": "^1.0.0",
    "es6-set": "^0.1.4",
    "object-assign": "^4.0.1",
    "parse5": "^2.1.5",
    "to-fast-properties": "^1.0.1"
  }
}
